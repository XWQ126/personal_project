#include <iostream>

using namespace std;
int n;
char map[100][100];
int main() {
    int max_num=0;  //记录每一次查找到的最大值
    int max_sum=0;  //用来比较每次查找的最大值的其中的最大值
    int sum=0;  //用来记录当前找到的已经放置的炮楼的数量
    int flag=0; //当作标记，判断当前该位置是否放置炮楼


}
